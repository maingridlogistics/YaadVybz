// iOS stub — Google Mobile Ads is not configured for iOS.
// react-native-google-mobile-ads is excluded from iOS autolinking via
// react-native.config.js. This component returns null so no ad banner
// is rendered and the native module is never accessed on iOS.
import React from 'react';

export default function AdBanner(): null {
  return null;
}
