// iOS stub — Google Mobile Ads is not configured for iOS.
// react-native-google-mobile-ads is excluded from iOS autolinking via
// react-native.config.js. This file ensures the native module is never
// imported at the JavaScript layer on iOS either.
export const BANNER_AD_UNIT_ID = '';
